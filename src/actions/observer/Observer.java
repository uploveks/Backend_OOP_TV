package actions.observer;

import inputdata.Notifications;

public class Observer {
    public void update(Notifications notification) {
        // code to update the observer based on the notification
    }
}
